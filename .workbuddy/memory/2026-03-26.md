# 2026-03-26 工作日志

## 投资策略模块（新品设计）

用户确认方案（11:21）：
- 按新功能开发，不动现有期权策略模块
- 个性化推荐：先收集用户画像（资金规模、风险偏好、投资目标、经验水平）
- 智能匹配：根据画像推荐适合的策略
- 全部策略可自由浏览（可筛选）

产品方案：
1. 新用户引导 + 5道题问卷 → 生成用户画像
2. 个性化推荐区 + 全部策略广场
3. 策略详情页：配置建议、标的示例、策略说明
4. 数据模型：UserProfile + Strategy

待出 PRD 产品需求文档。

---

## PRD 完成（11:29）

- 新建 `investment_strategy_prd.html`，包含：产品概述、核心概念、功能清单、用户流程、数据库设计、API设计、预置策略数据、界面原型、开发计划
- 用户确认：按新功能开发，不动现有期权策略模块
- 开发节奏：按 PRD 分三阶段进行
- 全面切换到 Yahoo Finance API（无需 Key，无限制）
- backend.py 新增 `_yf_fetch()`、`get_prices_batch()`（并发+缓存）、`get_market_data()`（SPY/QQQ/^VIX）
- `dashboard()` 接口每次调用实时更新持仓价格并写回数据库
- 风险预警改为基于真实数据动态计算
- 前端持仓表格新增「今日涨跌」列，VIX图表动态化，顶部显示市场状态（盘中/盘后）

## Bug修复（09:55-10:07）

### 问题1：个人偏好保存后持仓管理设置不同步
- 根因：`calc_portfolio` 函数硬编码 `target_cash_ratio: 35.0`，未使用传入参数
- 修复：改为 `target_cash_ratio: target_cash_ratio`（使用传入值）

### 问题2：刷新后正股持仓消失
- 根因：首次加载 dashboard 时，`_firstLoadComplete=false`，切到 manage 页面时走了 `renderManage()` 而非 `fetchData()`
- 修复：navigate 逻辑增强——当 `!dashData` 或无 `portfolio_holdings` 时自动触发 fetchData()，而非仅检查 `_isMockData`

### 问题3：资讯动态接入实时新闻
- 现有 3 条静态数据太旧，新增 `_get_news()` 函数实现：
  - 优先尝试 Yahoo Finance RSS（已挂）
  - 回退到本地 10 条精选财经新闻（NVDA/TSLA/美联储/比特币等）
  - 字段：标题、摘要、来源、时间戳、影响级别、关联股票
- API：`GET /api/news` 和 dashboard 返回的 news 都动态调用

## 个人中心功能
- 新建 `frontend/profile.html`：账户信息、投资偏好设置（现金/目标仓位）、修改密码、退出登录
- backend.py 新增 `POST /api/user/change-password` 接口（验证旧密码，bcrypt 哈希新密码）
- backend.py 新增 `/dashboard` 和 `/profile` 路由
- 主仪表盘左下角头像卡片改为可点击，跳转 `/profile`（个人中心）

## 刷新状态文字修复
- 成功刷新时显示「已更新 HH:MM:SS」而非「刚刚更新」
- 刷新失败时文字变红（`var(--danger)`），视觉上与成功状态明显区分

---

## 投资策略模块开发（11:34-12:00）

### 后端
- `init_db()` 新增三表：user_profiles、strategies、user_strategy_subscriptions
- `_seed_strategies()` 预置 11 个策略（保守→稳健→平衡→激进）
- 新增 API：
  - `GET/POST /api/strategy/profile` - 用户画像
  - `GET /api/strategy/recommend` - 个性化推荐
  - `GET /api/strategy/list` - 全部策略列表
  - `GET /api/strategy/{id}` - 策略详情
  - `POST /api/strategy/{id}/subscribe` - 订阅策略
  - `GET /api/strategy/subscriptions` - 我的订阅
- 匹配度算法：_calc_match_score() 基于资金规模、风险偏好、投资目标计算

### 前端
- 新建 `frontend/strategy.html`：
  - 用户画像展示 + 修改画像入口
  - 为您推荐区（匹配度展示）
  - 全部策略广场（支持风险等级筛选）
  - 5道题问卷弹窗（资金/风险/目标/经验/周期）
- 新建 `frontend/strategy_detail.html`：
  - 策略概览（风险/资金/收益）
  - 适用画像标签
  - 配置建议（资产类型/比例/标的）
  - 订阅按钮
- 路由：`/strategy`、`/strategy/{id}`

### 整合
- 侧边栏新增"投资策略"入口（带"新"标签）

## Bug修复（15:00-15:15）

### 问题1：风险评测提交无反应
- 根因：`strategy.html` 和 `strategy_detail.html` 的 API 请求未带认证头
- 修复：添加 `token` 变量和 `authHeaders()` 函数，为所有 `/api/strategy/*` 请求添加认证

### 问题2：测评完成后无推荐
- 根因：`_calc_match_score()` 算法逻辑有误，匹配度计算错误
- 修复：重写匹配算法，直接比对用户画像字段与策略标签

### 问题3：推荐为空时显示"完成测评后显示推荐"
- 修复：改为更友好的提示"暂无匹配策略，尝试调整画像或浏览全部策略"

### 已完成
- 投资策略页面已整合到主仪表盘（`claw_dashboard_enhanced.html`）
- 添加 `page-strategy` 页面区域，包含：用户画像卡片、为您推荐、全部策略、问卷弹窗
- 侧边栏导航改为使用 `navigate('strategy')` 切换页面
- 添加完整 JavaScript 逻辑：`loadStrategyData()`, `loadStrategyRecommendations()`, 问卷功能等
- 策略详情点击后在新标签页打开独立详情页

### Bug修复（22:00）
1. **修改画像按钮文案**：未完成测评时显示"风险测评"，已完成显示"修改画像"
2. **返回按钮链接**：策略详情页所有返回按钮改为 `/dashboard#/strategy`
3. **订阅功能修复**：`subscribe_strategy` API 中 `log_action` 调用缺少 `db` 参数导致死锁，已修复
