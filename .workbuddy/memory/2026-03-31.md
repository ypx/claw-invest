## 2026-03-31 修复记录

### 1. SPA路由刷新问题（已修复）

**问题**：页面刷新后总是跳转到"投资总览"页面，但地址栏URL保持不变

**根本原因**：`getHashPage()` 函数引用了未定义的 `PAGE_TITLES` 变量，导致总是返回默认的 'overview' 页面

**修复方案**：
- 文件：`/Users/nn/WorkBuddy/Claw/frontend/claw_dashboard_enhanced.html`
- 修改 `getHashPage()` 函数，移除对 `PAGE_TITLES` 的依赖
- 改为直接检查 DOM 元素 `document.getElementById('page-' + hash)` 是否存在
- 如果页面元素存在则返回对应页面，否则返回默认的 'overview'

**影响范围**：所有使用哈希路由的页面刷新场景

**测试建议**：在各个页面（投资策略、TSLA监控、持仓管理等）刷新浏览器，验证是否保持在当前页面

---

### 2. 持仓明细页面问题（已修复）

**问题1**：侧边栏"持仓明细"显示为"持仓管理"

**根本原因**：翻译文件 `zh.json` 中 `nav.holdings` = "持仓管理"，与页面实际标题不一致

**修复方案**：
- 文件：`/Users/nn/WorkBuddy/Claw/frontend/i18n/zh.json`
- 修改 `"holdings": "持仓管理"` → `"holdings": "持仓明细"`

---

**问题2**：持仓明细页面期权持仓没有数据

**根本原因**：`navigate()` 函数没有为 holdings 页面添加期权数据加载逻辑，仅在首次加载时通过 `renderAll()` 加载一次

**修复方案**：
- 文件：`/Users/nn/WorkBuddy/Claw/frontend/claw_dashboard_enhanced.html`
- 在 `navigate()` 函数中添加 holdings 页面切换时的数据加载：
```javascript
if (page === 'holdings') {
  setTimeout(loadOptionsOverview, 50);
}
```

---

**问题3**：期权持仓标题显示为"期权策略"

**根本原因**：holdings 页面使用了 `options.title` 翻译键，其值为"期权策略"（用于options页面），导致标题显示错误

**修复方案**：
- 添加新翻译键 `options.holdingsTitle` = "期权持仓"（zh.json 和 en.json）
- 修改 holdings 页面期权标题使用新翻译键：
```html
<span data-i18n="options.holdingsTitle">期权持仓</span>
```

---

**影响范围**：持仓明细页面（/dashboard#/holdings）

**测试建议**：
1. 访问持仓明细页面，验证侧边栏显示"持仓明细"
2. 验证期权持仓部分标题显示"期权持仓"
3. 验证期权数据正确加载并显示
4. 刷新页面，验证所有内容保持正确

---

### 3. 持仓明细期权数据未显示（2026-03-31 修复）

**问题**：持仓明细页面期权持仓表格没有数据

**根本原因**：`loadOptionsOverview()` 函数中存在 JavaScript 错误，使用了未定义的变量 `pnlVal`

**错误位置**：第 4574 行
```javascript
// 错误代码
${pnlVal>=0?'+':''}$${Math.abs(pnlVal).toFixed(2)}

// 正确代码（应使用 pnl 变量）
${pnl>=0?'+':''}$${Math.abs(pnl).toFixed(2)}
```

**修复方案**：
- 文件：`/Users/nn/WorkBuddy/Claw/frontend/claw_dashboard_enhanced.html`
- 修改 `pnlVal` → `pnl`

**影响范围**：期权持仓表格渲染

**测试建议**：
1. 清除浏览器缓存（Ctrl+Shift+R / Cmd+Shift+R）
2. 访问持仓明细页面
3. 验证期权持仓表格正确显示数据
4. 检查浏览器控制台是否有 JavaScript 错误

---

### 4. 后端生成内容的国际化问题（2026-03-31 修复）

**问题**：投资总览、健康评估、TSLA页面的动态内容在英文模式下显示中文

**根本原因**：
1. 后端API返回的中文文本没有完整的翻译映射
2. TSLA页面缺少翻译机制
3. 翻译映射表 `_healthTextMap` 和 `_alertI18nMap` 不完整

**修复方案**：

#### 4.1 扩展健康评估翻译映射
**文件**：`claw_dashboard_enhanced.html`

扩展 `_healthTextMap` 对象，添加更多中文文本映射：
```javascript
const _healthTextMap = {
  // 优势（6个）
  '持仓分散度良好': 'health.txt.strengthDiversification',
  '风险控制措施到位': 'health.txt.strengthRisk',
  '投资标的质量优秀': 'health.txt.strengthQuality',
  '现金管理稳健': 'health.txt.strengthCashMgmt',
  '长期价值投资': 'health.txt.strengthLongTerm',
  '资产配置均衡': 'health.txt.strengthBalanced',
  
  // 劣势（7个）
  '现金比例过高（59.2%）': 'health.txt.weaknessCash',
  '现金比例过高': 'health.txt.weaknessCashHigh',
  '科技股集中度过高': 'health.txt.weaknessTech',
  '部分持仓估值偏高': 'health.txt.weaknessValuation',
  '行业集中度过高': 'health.txt.weaknessSector',
  '仓位过重': 'health.txt.weaknessOverweight',
  '波动率偏高': 'health.txt.weaknessVolatile',
  
  // 建议（10个）
  '优化现金使用，考虑sell put策略': 'alerts.optimizeCash',
  '适当降低科技股仓位': 'health.txt.recReduceTech',
  '增加防御性行业配置': 'alerts.increaseDefensive',
  '等待市场回调': 'health.txt.recWaitPullback',
  '分批建仓': 'health.txt.recScaleIn',
  '保持现金储备': 'health.txt.recKeepCash',
  '考虑止损': 'health.txt.recStopLoss',
  '长期持有': 'health.txt.recHoldLong',
  '关注宏观': 'health.txt.recWatchMacro',
  '调整仓位': 'health.txt.recAdjustPosition'
};
```

#### 4.2 为TSLA页面添加翻译机制
**文件**：`claw_dashboard_enhanced.html`

1. 创建TSLA翻译映射表：
```javascript
const _tslaTextMap = {
  // 护城河标题（7个）
  '品牌与用户忠诚度': 'tsla.moatTitle1',
  '垂直整合与成本控制': 'tsla.moatTitle2',
  '充电网络与生态系统': 'tsla.moatTitle3',
  '数据与软件定义汽车': 'tsla.moatTitle4',
  '4680电池与供应链': 'tsla.moatTitle5',
  '能源业务协同': 'tsla.moatTitle6',
  '马斯克的执行力': 'tsla.moatTitle7',
  
  // 护城河详情（7个）
  '特斯拉车主复购率高达 70%，远超传统车企的 30-40%': 'tsla.moatDetail1',
  '从电池到整车到销售，自主掌控核心环节，毛利率持续领先': 'tsla.moatDetail2',
  '全球超充网络超 5 万桩，是竞争对手难以复制的护城河': 'tsla.moatDetail3',
  'OTA升级、自动驾驶数据闭环，让汽车越用越值钱': 'tsla.moatDetail4',
  '自研电池技术降低 14% 成本，供应链深度绑定': 'tsla.moatDetail5',
  '储能与汽车业务协同，打造「能源+交通」闭环': 'tsla.moatDetail6',
  '从Roadster到Model S/3/X/Y到Cybertruck，持续兑现承诺': 'tsla.moatDetail7',
  
  // 护城河数据点（7个）
  '全球电动车市占率 20%，美国 50%': 'tsla.moatData1',
  '单车毛利率 19% vs 丰田 12%、福特 8%': 'tsla.moatData2',
  '超充网络 5 万桩，覆盖 50+ 国家': 'tsla.moatData3',
  'FSD累计里程 10 亿英里，数据领先': 'tsla.moatData4',
  '4680电池能量密度+16%，成本-14%': 'tsla.moatData5',
  '储能业务年增速 60%，毛利 25%': 'tsla.moatData6',
  '过去 10 年交付量 CAGR 50%': 'tsla.moatData7',
  
  // 业务前景标题（6个）
  'Model 3/Y 持续放量': 'tsla.outlookTitle1',
  'Cybertruck 量产爬坡': 'tsla.outlookTitle2',
  'FSD 监管突破': 'tsla.outlookTitle3',
  '4680 电池量产': 'tsla.outlookTitle4',
  '能源业务翻倍': 'tsla.outlookTitle5',
  '机器人业务启动': 'tsla.outlookTitle6',
  
  // 业务前景描述（6个）
  '全球Model 3/Y年产能已突破200万辆，仍有增长空间': 'tsla.outlookDesc1',
  'Cybertruck预订超100万辆，德州工厂产能爬坡中': 'tsla.outlookDesc2',
  'FSD V12采用端到端AI，监管有望2024年突破': 'tsla.outlookDesc3',
  '4680电池良率提升至90%，成本优势即将显现': 'tsla.outlookDesc4',
  '储能业务年增速60%，毛利率25%，成为第二增长曲线': 'tsla.outlookDesc5',
  'Optimus机器人2024年小批量生产，潜在市场万亿级': 'tsla.outlookDesc6'
};
```

2. 创建翻译函数：
```javascript
function translateTslaText(text) {
  const key = _tslaTextMap[text];
  if (!key) return text;
  return t(key, text);
}
```

3. 更新渲染函数：
- `renderTslaMoat()`: 使用 `translateTslaText()` 翻译 title, detail, data_point
- `renderTslaOutlook()`: 使用 `translateTslaText()` 翻译 horizon, title, desc, tags

#### 4.3 补充翻译文件
**文件**：`en.json` 和 `zh.json`

为TSLA添加33个新的翻译键，包括：
- 7个护城河标题
- 7个护城河详情
- 7个护城河数据点
- 6个业务前景标题
- 6个业务前景描述

**影响范围**：
- 投资总览页面：AI建议、风险预警
- 健康评估页面：优势、待改进、AI优化建议
- TSLA页面：护城河、业务前景

**测试建议**：
1. 清除浏览器缓存
2. 切换到英文模式
3. 访问各页面验证内容显示为英文
4. 切换到中文模式验证内容显示为中文
5. 检查浏览器控制台是否有错误

---

### 5. 投资策略中心标签国际化修复（2026-03-31 修复）

**问题**：Investment Strategy Center 的 Your Investment Profile 四个标签在英文模式下显示中文

**根本原因**：
后端返回的中文值（如"保守"、"小白"、"短期"）直接用于拼接翻译键 `strategy.profileTag${value}`，但 en.json 中实际的翻译键是 `strategy.profileTagConservative`、`strategy.profileTagBeginner` 等英文形式。

**修复方案**：
**文件**：`/Users/nn/WorkBuddy/Claw/frontend/strategy.html`

添加中文到英文翻译键的映射：
```javascript
const riskTagMap = { '保守': 'Conservative', '稳健': 'Steady', '平衡': 'Balanced', '激进': 'Aggressive' };
const expTagMap = { '小白': 'Beginner', '有经验': 'Experienced', '专业': 'Expert' };
const horizonTagMap = { '短期': 'ShortTerm', '中期': 'MidTerm', '长期': 'LongTerm' };
```

更新标签渲染代码，使用映射后的英文键：
```javascript
const tags = [
  t(`strategy.profileTag${riskTagMap[data.risk_tolerance]}`, data.risk_tolerance) + t('strategy.profileTagRiskType', '型'),
  t(`strategy.profileTag${expTagMap[data.experience_level]}`, data.experience_level),
  t(`strategy.profileTag${horizonTagMap[data.time_horizon]}`, data.time_horizon) + t('strategy.profileTagInvestment', '投资')
];
```

**影响范围**：Investment Strategy Center 页面

**测试建议**：
1. 完成风险测评
2. 切换到英文模式
3. 验证 Your Investment Profile 的四个标签显示为英文
4. 切换回中文模式验证显示为中文

---

### 6. TSLA Outlook标签国际化修复（2026-03-31 修复）

**问题**：TSLA Monitor 页面业务前景（outlook）标签显示为中文（如"出行"、"储能"、"利好"）

**根本原因**：
后端返回的 outlook 数据包含中文标签数组，前端渲染时未进行国际化处理：
```javascript
tags: ['Robotaxi', 'AI', '出行'],  // 中文标签
```

**修复方案**：
**文件 1**：`/Users/nn/WorkBuddy/Claw/frontend/i18n/en.json` 和 `zh.json`

添加 outlook 标签翻译键（17个）：
```json
"outlookTagRobotaxi": "Robotaxi",
"outlookTagAI": "AI",
"outlookTag出行": "Mobility",  // 英文翻译
"outlookTag储能": "Energy Storage",
"outlookTagMegapack": "Megapack",
"outlookTag利好": "Bullish",
"outlookTagFSD": "FSD",
"outlookTagSaaS": "SaaS",
"outlookTag订阅": "Subscription",
"outlookTagOptimus": "Optimus",
"outlookTag机器人": "Robotics",
"outlookTag长期": "Long-term",
"outlookTag竞争": "Competition",
"outlookTag比亚迪": "BYD",
"outlookTag风险": "Risk",
"outlookTag政治": "Political",
"outlookTag品牌": "Brand"
```

**文件 2**：`/Users/nn/WorkBuddy/Claw/frontend/claw_dashboard_enhanced.html`

扩展 `_tslaTextMap` 翻译映射表：
```javascript
const _tslaTextMap = {
  // ... 现有内容 ...
  
  // Outlook 标签（17个）
  'Robotaxi': 'tsla.outlookTagRobotaxi',
  'AI': 'tsla.outlookTagAI',
  '出行': 'tsla.outlookTag出行',
  '储能': 'tsla.outlookTag储能',
  'Megapack': 'tsla.outlookTagMegapack',
  '利好': 'tsla.outlookTag利好',
  'FSD': 'tsla.outlookTagFSD',
  'SaaS': 'tsla.outlookTagSaaS',
  '订阅': 'tsla.outlookTag订阅',
  'Optimus': 'tsla.outlookTagOptimus',
  '机器人': 'tsla.outlookTag机器人',
  '长期': 'tsla.outlookTag长期',
  '竞争': 'tsla.outlookTag竞争',
  '比亚迪': 'tsla.outlookTag比亚迪',
  '风险': 'tsla.outlookTag风险',
  '政治': 'tsla.outlookTag政治',
  '品牌': 'tsla.outlookTag品牌'
};
```

**文件 3**：`/Users/nn/WorkBuddy/Claw/frontend/claw_dashboard_enhanced.html`

在 `renderTslaOutlook()` 函数中使用翻译函数：
```javascript
${o.tags.map(tag => `<span>#${translateTslaText(tag)}</span>`).join('')}
```

**影响范围**：TSLA Monitor 页面的业务前景标签

**测试建议**：
1. 清除浏览器缓存
2. 访问 TSLA Monitor 页面
3. 切换到业务前景 Tab
4. 验证所有标签显示为英文（如 #Mobility、#Bullish）
5. 切换回中文模式验证显示为中文

---

### 7. 策略描述国际化修复（2026-03-31 修复）

**问题**：Investment Strategy Center 的策略卡片描述在英文模式下显示中文

**根本原因**：
后端返回的策略描述（如"以美股核心科技股为主，强调长期持有和现金管理..."）直接渲染，未进行国际化处理。

**修复方案**：
**文件 1**：`/Users/nn/WorkBuddy/Claw/frontend/strategy.html`

添加策略描述翻译映射：
```javascript
const _strategyDescMap = {
  '以美股核心科技股为主，强调长期持有和现金管理，适合风险厌恶型投资者。配置重点在NVDA、GOOGL等优质标的，保留充足现金应对波动。': 'strategy.conservativeCoreDesc',
  '核心仓位+成长仓位的平衡配置，在稳健基础上追求增长。包含AI基础设施、云计算等前沿赛道，同时通过sell put策略增强收益。': 'strategy.balancedGrowthDesc',
  '重仓AI和前沿科技，追求高回报。适合能承受较大波动的投资者，包含量子计算、加密货币等高风险高回报标的。': 'strategy.aggressiveTechDesc',
  '专注于通过sell put期权策略获取权利金收入，特别适合震荡市或温和上涨市场。优先选择TSLA等高流动性标的，设置合理行权价。': 'strategy.sellPutIncomeDesc',
  '将加密货币作为资产配置的一部分，通过BTC和ETH获取数字黄金收益。建议通过ETF或小额直接配置，控制总体仓位。': 'strategy.cryptoHedgeDesc',
  '提前布局量子计算赛道，以IONQ为核心标的。这是5-10年的长期赛道，适合能承受高波动、追求爆发性增长的投资者。': 'strategy.quantumFutureDesc'
};

function translateStrategyDesc(text) {
  if (!text) return '';
  const key = _strategyDescMap[text];
  if (!key) return text;
  return t(key, text);
}
```

**文件 2**：`/Users/nn/WorkBuddy/Claw/frontend/strategy.html`

在 `createStrategyCard()` 函数中使用翻译函数：
```javascript
<p class="text-sm text-gray-500 mt-1">${translateStrategyDesc(s.description)?.substring(0, 60)}...</p>
```

**文件 3**：`/Users/nn/WorkBuddy/Claw/frontend/i18n/en.json` 和 `zh.json`

添加6个策略描述的翻译键：
```json
"conservativeCoreDesc": "Focus on US core tech stocks, emphasize long-term holding and cash management, suitable for risk-averse investors. Allocation focused on quality targets like NVDA, GOOGL, with ample cash reserves for volatility.",
"balancedGrowthDesc": "Core positions + growth positions for balanced allocation, pursuing growth on a stable foundation. Includes AI infrastructure, cloud computing and other cutting-edge tracks, while enhancing returns through sell put strategy.",
"aggressiveTechDesc": "Heavy positions in AI and cutting-edge technology, pursuing high returns. Suitable for investors who can withstand greater volatility, including high-risk, high-return targets such as quantum computing and cryptocurrency.",
"sellPutIncomeDesc": "Focus on generating premium income through sell put options strategy, especially suitable for volatile or moderately rising markets. Prioritize highly liquid targets like TSLA, set reasonable strike prices.",
"cryptoHedgeDesc": "Include cryptocurrency as part of asset allocation, gain digital gold returns through BTC and ETH. Recommend ETF or small direct allocation, control overall position size.",
"quantumFutureDesc": "Early layout of quantum computing track, with IONQ as the core target. This is a 5-10 year long-term track, suitable for investors who can withstand high volatility and pursue explosive growth."
```

**影响范围**：Investment Strategy Center 的策略列表和推荐策略

**测试建议**：
1. 访问 Investment Strategy Center
2. 切换到英文模式
3. 验证所有策略描述显示为英文
4. 点击策略卡片查看详情页，验证描述完整显示为英文
5. 切换回中文模式验证显示为中文

---

### 8. 国际化修复总结（2026-03-31）

**本次修复共解决6个核心问题**：
1. ✅ Investment Strategy Center 的 Your Investment Profile 四个标签
2. ✅ TSLA Monitor 顶部标题和介绍
3. ✅ TSLA outlook 标签的 # 前缀和中文显示
4. ✅ Strategy 页面策略描述
5. ✅ Risk Alert Center（已正确国际化，无需修复）
6. ✅ 补充所有缺失的 i18n 翻译键

**新增翻译键统计**：
- en.json: +40 个键（17个outlook标签 + 6个策略描述 + 其他）
- zh.json: +40 个键
- claw_dashboard_enhanced.html: +17 个TSLA outlook标签映射
- strategy.html: +6 个策略描述映射 + 3个映射表

**关键修复模式**：
1. **映射表模式**：创建中文到翻译键的映射（如 `_strategyDescMap`、`_tslaTextMap`）
2. **动态内容处理**：增强翻译函数处理含变量的文本（如 `translateAlertField`）
3. **键名规范化**：确保翻译键使用英文命名（如 `profileTagConservative` 而非 `profileTag保守`）

**重要提醒**：
- 清除浏览器缓存（Ctrl+Shift+R / Cmd+Shift+R）才能看到最新更改
- 所有后端返回的中文文本都必须有对应的翻译映射
- 动态生成的内容（含变量）需要特殊处理

#### 5.1 扩展健康评估翻译映射
**文件**：`claw_dashboard_enhanced.html`

扩展 `_healthTextMap` 到30+项，包含所有后端返回的中文：
- 新增：'核心持仓聚焦'、'现金比例偏离目标'、'科技股集中度较高'
- 新增：'通过sell put策略优化现金使用'、'维持现有现金配置'等动态建议
- 新增：'定期检视核心持仓基本面'、'关注AI/量子计算赛道新机会'

**修复健康评估页面"建议X"显示**：
- 将 `${t('health.suggestion', '建议')} ${i+1}` 改为 `${t('health.suggestion', 'Suggestion')} ${i+1}`

#### 5.2 扩展风险预警翻译映射
**文件**：`claw_dashboard_enhanced.html`

扩展 `_alertI18nMap` 到20+项，处理所有alert类型：
- 新增：'现金比例偏低'、'利率/宏观风险'
- 新增：动态description处理（含变量替换）
- 新增：所有action翻译

增强 `translateAlertField()` 函数，支持动态内容：
```javascript
// 处理现金比例description中的变量
if (field === 'description' && text.includes('现金比例')) {
  // 提取cashRatio和targetRatio并翻译
}
```

#### 5.3 扩展TSLA页面翻译映射
**文件**：`claw_dashboard_enhanced.html`

扩展 `_tslaTextMap` 到70+项，包含所有TSLA内容：
- 新增：所有后端返回的护城河标题、详情、数据点（14套）
- 新增：所有业务前景标题和描述（12套）
- 新增：Sell Put指南所有动态内容（why, tips, current_suggestion）
- 新增：遗漏的tips：'TSLA 大幅回调（5-10%）后是最佳开仓窗口'、'每次使用资金不超过总仓位 30%'
- 新增：行权价建议类型（保守型、稳健型、进取型）

增强 `translateTslaText()` 函数，支持动态内容：
```javascript
// 处理当前价格建议（含价格变量）
if (text.includes('当前 TSLA 价格') && text.includes('建议选择')) {
  // 提取price, conservativeStrike, moderateStrike并翻译
}
```

**修复业务前景horizon标签**：
- 新增horizon翻译：'持续' → 'Ongoing', '当前' → 'Current'
- 在 `renderTslaOutlook()` 中添加 `translateHorizon()` 函数

#### 5.4 补充翻译文件
**文件**：`frontend/i18n/en.json` 和 `zh.json`

为TSLA添加50+个新的翻译键，包括：
- 14个护城河标题
- 14个护城河详情
- 14个护城河数据点
- 12个业务前景标题和描述
- Sell Put指南所有字段（why, timing, conditions, types等）
- horizon字段：'horizonOngoing', 'horizonCurrent', 'horizon2026Plus'

#### 5.5 修复投资策略页面
**文件**：`claw_dashboard_enhanced.html`

修复策略卡片中的中文内容：
- 策略名称：添加翻译 `${t(\`strategy.${s.id}.name\`, s.name)}`
- 策略描述：添加翻译 `${t(\`strategy.${s.id}.description\`, s.description || '')}`
- 策略标签：添加翻译 `tags.map(tag => t(\`strategy.${s.id}.tag.${tag}\`, tag))`

**补充策略翻译文件**：
为所有6个策略添加完整翻译：
- conservative-core（保守核心配置）
- balanced-growth（平衡增长策略）
- aggressive-tech（科技激进策略）
- sell-put-income（Sell Put收益策略）
- crypto-hedge（加密资产配置）
- quantum-future（量子计算前瞻）

每个策略包含：name, description, 所有tags

**影响范围**：
- 投资总览页面：风险预警、AI建议
- 健康评估页面：优势、待改进、AI优化建议
- TSLA页面：护城河、业务前景、Sell Put指南（所有Tab）
- 投资策略页面：策略描述和标签
- 持仓明细页面：期权持仓空状态提示

**测试建议**：
1. **必须清除浏览器缓存**（Ctrl+Shift+R / Cmd+Shift+R）
2. 切换到英文模式
3. 访问所有页面验证内容显示为英文
4. 检查浏览器控制台是否有JavaScript错误
5. 验证动态内容（含变量）正确显示
6. 切换回中文模式验证内容显示为中文
