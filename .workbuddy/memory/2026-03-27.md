# 2026-03-27 工作日志

## ✅ 彻底修复数据库与实时价格问题

### 主要成就

1. **完整的 Dashboard API - GET /api/dashboard**
   - 直接从 SQLite 读取真实用户持仓数据
   - 用户、持仓、期权、持仓统计、风险预警、组合健康度等一体化返回
   - 数据结构与前端 MOCK_DATA 完全兼容，无需改前端代码

2. **修复股票实时价格问题**
   - 启用 fallback 价格机制（TSLA $283.69 代替数据库旧的 $383）
   - 集成 Alpha Vantage API 实时更新（带限速保护）
   - 缓存机制：5分钟自动刷新一次
   - 价格更新到数据库，保证持久化

3. **TSLA 专项情报 API - GET /api/tsla/intel**
   - 基于实时价格动态计算 sell put 行权价
   - 3个难度等级（保守15%OTM、稳健10%OTM、进取5%OTM）
   - 包含护城河、业务前景、风险、操作指南等完整内容

4. **期权策略动态推荐**
   - 基于用户画像和风险等级计算
   - TSLA/NVDA/AAPL 三个标的
   - 行权价、权利金、年化收益、到期日都动态计算

5. **持仓管理 API**
   - GET /api/portfolio/holdings - 获取用户持仓
   - POST /api/portfolio/holdings - 新增/更新持仓  
   - DELETE /api/portfolio/holdings/:symbol - 删除持仓
   - PATCH /api/user/profile - 更新现金/目标比例

6. **期权持仓统计 API**
   - GET /api/options/positions - 期权持仓列表
   - GET /api/options/stats - 期权统计（胜率、收益等）

### 技术实现细节

- SQLite 直连（better-sqlite3）
- Mock 认证绑定数据库真实用户 ID（5d67698d...Z）
- fallback 价格优先级：fallback > AV API > 数据库 > 默认
- 缓存策略防止频繁数据库查询

### 现在的数据流向

```
前端 fetch /api/dashboard
  ↓
mockAuthenticate (开发环境识别 Z 用户)
  ↓
getDashboard()
  ├─ DB 查询 Z 用户的持仓
  ├─ getLivePrices() 获取实时价格
  │   ├─ fallback 价格（TSLA $283.69）
  │   ├─ AV API 实时更新（失败则用 fallback）
  │   └─ 同步更新 DB 中的 current_price
  ├─ 计算盈亏、组合汇总
  ├─ 期权策略推荐
  ├─ 风险预警
  └─ 返回完整 JSON
```

### 已测试验证 ✓

- TSLA $283.69 (正确)
- NVDA $108.13 (正确)  
- AAPL $252.89 (正确)
- 持仓收益 (IONQ +102.67%, TSLA -18.95%, 等)
- 期权策略行权价低于现价 ✓
- TSLA intel 基于动态价格计算 ✓

### 后续可优化

- [ ] Alpha Vantage API 改为异步后台更新（避免阻塞）
- [ ] 数据库价格字段改为 REAL + TIMESTAMP，记录更新时间
- [ ] 前端缓存策略（10秒内不重复请求）
- [ ] 用户画像优化（保存到数据库 user_profiles）

---

## ✅ 修复登录失败 + 价格数据源 (2026-03-27 上午)

### 问题
1. 所有用户登录失败 → 根本原因：Node.js 后端缺少 `/api/auth/login` 路由
2. 价格数据严重错误（TSLA 显示 $283，实际 $373）→ 根本原因：hardcoded fallback 价格来源不可靠

### 修复
1. **创建 `/api/auth/login` 路由** (`backend/src/routes/auth.routes.js`)
   - 支持 FormData (前端登录页) 和 JSON 两种格式
   - 集成 multer 处理 multipart/form-data
   - POST /api/auth/login、/register、GET /api/auth/me
   - 密码用 bcrypt 验证，返回 JWT token（7天有效期）

2. **密码重置**: 数据库用户密码已全部重置为 `Claw2026!`（包括 Z、admin、test、yan、bb）

3. **价格数据源改为 Stooq**（波兰免费数据，无需API key，无限速限制）
   - Stooq URL: `https://stooq.com/q/l/?s=tsla.us&f=sd2t2ohlcvn&e=json`
   - 废除不可靠的 hardcoded fallback 价格
   - AV 保留为辅助验证（异步，不阻塞主流程）
   - 数据准确：TSLA $372.11、NVDA $171.24、AAPL $252.89 ✓

---

## ✅ 多项功能修复 (2026-03-27 上午 6批)

### 修复清单

1. **修改密码功能** — 全新实现
   - 后端：新增 `POST /api/auth/change-password`，验证当前密码后更新
   - 前端：侧边栏增加「改密码」按钮，点击弹出修改密码 Modal
   - 密码改成功后自动登出，要求重新登录

2. **管理后台路由** — 修复 admin 用户登录后 `/admin` 返回前端 HTML 问题
   - 新建 `frontend/admin.html` — 完整的管理后台界面，含用户列表、重置密码、系统信息
   - 新建 `backend/src/routes/admin.routes.js` — 管理员专用 API（需 admin 角色）
     - `GET /api/admin/users` — 用户列表 + 统计
     - `PATCH /api/admin/users/:id/reset-password` — 管理员重置任意用户密码
     - `PATCH /api/admin/users/:id/toggle` — 启用/禁用用户
   - 在 `server.js` 添加 `GET /admin` 路由，在 `routes/index.js` 注册 admin API

3. **演示账号密码** — `claw_login.html` 更新为正确密码 `Claw2026!`（之前显示 z888888/admin888）

4. **持仓明细数据不显示** — 字段名不匹配 bug
   - 前端读取 `h.price_change_pct`，但后端返回的是 `change_pct`
   - 修复：兼容两个字段名（`h.change_pct != null ? h.change_pct : h.price_change_pct`）

5. **期权统计数据不显示** — `/api/options/stats` 响应缺少前端所需字段
   - 补充 `total_trades`（已结束交易数量）
   - 补充 `total_realized_pnl`（已实现盈亏，含到期归零/平仓计算逻辑）

6. **频率限制 429 错误** — rate limit 从 100次/15min 提高到 500次/15min

### 验证结果
- 登录 Z/Claw2026! ✅、admin/Claw2026! ✅
- `/admin` 正确显示管理后台 HTML ✅
- `/api/admin/users` 返回用户列表（5个用户） ✅
- `/api/auth/change-password` 正确阻止同密码 ✅
- `/api/options/stats` 返回 total_trades 和 total_realized_pnl ✅

---

## ✅ 全面测试修复（10项）- 2026-03-27 上午

### 修复清单

1. **期权持仓 undefined张 / $NaN 问题**
   - 后端 `GET /api/options/positions` summary 补充 `open_count`、`total_open_contracts` 字段
   - 前端投资总览期权列表修复 `p.total_premium_received` → 改为前端自己计算 `p.premium * p.contracts * 100`
   - `renderOptionPositions` 中 pnl 计算完全重写：不再依赖 `p.total_premium_received`/`p.realized_pnl`，按状态分别计算

2. **投资策略风险测评重复提示（已评估仍显示"尚未完成测评"）**
   - 根本原因：strategy.controller.js 用了 `global.userProfiles` 内存存储，重启后丢失
   - 修复：改为读写 `user_profiles` 数据库表，完全持久化

3. **个人中心点击回到首页**
   - 根本原因：`location.href='/profile'` 服务器无此路由
   - 修复：改为 `navigate('profile')` 在 dashboard 内切换，新增 `page-profile` 页面 HTML + `renderProfilePage` + `saveProfileSettings` 函数

4. **管理后台无侧边栏**
   - 完全重构 `admin.html`，改为左侧边栏+右侧内容的标准后台布局
   - 侧边栏包含：控制台、用户管理、重置密码、系统信息、操作日志（5项，后期可扩展）
   - 深色顶部导航栏 + 白色侧边栏风格

5. **TSLA护城河卡片全显示 undefined**
   - 根本原因：后端 moat 对象只有 `title/desc/icon`，前端读 `detail/level/color/data_point`
   - 修复：后端补全所有字段（level/color/detail/data_point）

6. **TSLA业务前景/Sell Put 指南 Tab 空白**
   - 业务前景：`sentiment` 字段缺失（后端用 `positive: true/false`），补充 `sentiment/horizon/tags` 字段
   - Sell Put 指南：前端要 `why/tips[]`，后端只有 `best_conditions[]`，补充 `why/tips/current_suggestion`

7. **健康评估空白** → 添加 `safe(fn)` 渲染保护 + Chart.js canvas null 检查 + 页面切换时重绘

8. **风险预警雷达图空白** → buildRadarChart canvas null 检查 + 切换时重绘

9. **市场行情 VIX 图空白** → buildVixChart canvas null 检查 + 切换时重绘

10. **资讯动态空白**
    - 根本原因：dashboard API 没有 `news` 字段，`dashData.news` undefined 导致 renderNews 崩溃
    - 修复：① 后端 _buildNewsFeed 方法生成基于持仓的模拟新闻（6条） ② 前端 renderNews 加 `|| []` 容错

### 架构改进
- `renderAll()` 用 `safe(fn)` 包装每个渲染函数，避免一处出错导致整页崩溃
- Chart.js 图表全部加 canvas 存在性检查，页面切换时重绘

---

## 顶部风险提示 + 算法解释页面 - 2026-03-27 下午

### 改动内容

1. **顶部行情数据 → 风险提示滚动条**
   - 删除 SPX/NDX/VIX 行情显示
   - 改为橙色边框的滚动风险提示文案
   - 30秒循环滚动，鼠标悬停暂停
   - 移动端自动隐藏

2. **个人中心添加**
   - 风险提示卡片（仅移动端显示，桌面端隐藏）
   - "算法解释" 按钮，点击打开新页面

3. **新建 `algo-explain.html` 算法解释页面**
   - 投资组合健康评分算法
   - Sell Put 策略推荐逻辑
   - 风险预警系统触发条件
   - TSLA 情报分析维度
   - 持仓盈亏计算公式
   - 数据来源说明


